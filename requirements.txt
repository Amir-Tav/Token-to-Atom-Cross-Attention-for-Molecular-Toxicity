# ── Tell pip where to find the CUDA wheels for PyTorch ──────────────────────
--extra-index-url https://download.pytorch.org/whl/cu118

# ── Core scientific stack ───────────────────────────────────────────────────
numpy>=1.26
pandas>=2.2
matplotlib>=3.9
requests>=2.32
pillow>=10.3            # RDKit SVG → PNG and Streamlit image support

# ── Chemistry toolkit ───────────────────────────────────────────────────────
rdkit-pypi==2023.3.1b1  # latest wheel on PyPI; includes MolLogP, MolWt, …

# ── Machine‑learning / explainability ───────────────────────────────────────
torch==2.2.*+cu118
torchvision==0.17.*+cu118
torchaudio==2.2.*+cu118
transformers>=4.40
shap>=0.45
scikit-learn>=1.5
joblib>=1.4

# ── Web UI and extras ───────────────────────────────────────────────────────
streamlit>=1.35
chembl_webresource_client>=0.10
pypdf>=4.3
